# ATAG Jobs — QR Attendance

## Local Quick Start
```bash
docker compose up -d
npm run db:generate
npm run db:migrate
npm run db:seed
npm run dev:api
npm run dev:web
```

API: http://localhost:4000
Web: http://localhost:3000

### Demo users
- pm@example.com (PM)
- admin@example.com (ADMIN)
- alice@example.com (PART_TIMER)
